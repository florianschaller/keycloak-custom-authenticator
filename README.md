# Start keycloak and set configuration using terraform

```shell
docker compose up -d --build
```

# Stop keycloak

```shell
docker compose down
```

# Stop keycloak and remove the volume

```shell
docker compose down -v
```
